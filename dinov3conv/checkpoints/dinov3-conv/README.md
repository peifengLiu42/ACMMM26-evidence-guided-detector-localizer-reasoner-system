# DINOv3Conv Checkpoint

Place `pytorch_model.bin` here before evaluation or prediction.

The file must be the full `DINOv3TamperLocalizer.state_dict()`: DINO backbone,
classifier head, feature projection, and mask decoder.
